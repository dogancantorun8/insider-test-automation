---
# Ansible Requirements
# Bu dosya Ansible collection'lari ve role'lari tanimlar

collections:
  - name: amazon.aws
    version: ">=5.0.0"
  - name: kubernetes.core
    version: ">=2.4.0"
  - name: community.general
    version: ">=5.0.0"
  - name: ansible.posix
    version: ">=1.4.0"

roles:
  - name: geerlingguy.docker
    version: "6.0.0"
  - name: geerlingguy.pip
    version: "2.0.0"
  - name: geerlingguy.security
    version: "1.9.0"
